# ── Streamlit ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── LangChain ecosystem ────────────────────────────────────────────────────────
# Let pip resolve langchain-core version — do NOT pin it
# langchain-chroma 0.2.4 needs >=0.3.60, langchain 0.3.25 needs >=0.3.58
# Unpinning langchain-core lets the resolver pick a compatible version (0.3.60+)
langchain==0.3.25
langchain-community==0.3.24
langchain-openai==0.3.16
langchain-text-splitters==0.3.8
langchain-chroma==0.2.4

# ── LangGraph ──────────────────────────────────────────────────────────────────
langgraph==0.4.8

# ── Vector store ───────────────────────────────────────────────────────────────
chromadb>=0.6.0

# ── PDF loading ────────────────────────────────────────────────────────────────
pymupdf==1.25.5

# ── Web search — use langchain-tavily (TavilySearchResults is deprecated) ──────
langchain-tavily>=0.1.0

# ── SQL ────────────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0

# ── Pydantic ───────────────────────────────────────────────────────────────────
pydantic>=2.0.0
